# API 契约占位

当前仓库已创建后端工程壳，但尚未生成业务接口契约。

后续建议在本文件中维护：

- 登录与刷新令牌接口
- 员工、学生、课程、薪酬、快捷链接、日志接口
- 统一错误码
- 分页与筛选参数
- 上传接口约定

当前后端基础可从以下文件继续扩展：

- `apps/api/src/app.module.ts`
- `apps/api/src/modules/*`
- `apps/api/prisma/schema.prisma`

